//how to restrict users choice 
enum SeatChoice {
    AISLE,   //(by default 0)
    MIDDLE = 3,
    WINDOW,    // the numbers will be followed one after another (4)
    FOURTH      //5
}

const hcSeat = SeatChoice.AISLE;

export{}
